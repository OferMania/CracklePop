# CracklePop

A CracklePop implementation (a FizzBuzz variant) — prints 1 to 100, replacing
multiples of 3 with "Crackle", multiples of 5 with "Pop", and multiples of
both with "CracklePop".

To build CracklePop, do:

```
cargo build
```

To run CracklePop, do:

```
cargo run
```
